<Page x:Class="RoguelikeWPF.Pages.GamePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d"
      Title="Игра">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- верхняя панель -->
            <RowDefinition Height="*"/>
            <!-- картинка комнаты -->
            <RowDefinition Height="Auto"/>
            <!-- кнопки действий -->
            <RowDefinition Height="200"/>
            <!-- низ (инвентарь + лог) -->
        </Grid.RowDefinitions>

        <!-- === ВЕРХНЯЯ ПАНЕЛЬ (теперь с Grid, чтобы ничего не налезало) === -->
        <Grid Grid.Row="0" Background="#FF1E1E1E" Margin="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="80"/>
                <ColumnDefinition Width="300"/>
                <ColumnDefinition Width="120"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <TextBlock Text="Этаж" FontSize="20" Foreground="White" VerticalAlignment="Center" Grid.Column="0"/>
            <TextBlock x:Name="tbFloor" Text="1" FontSize="22" Foreground="Lime" VerticalAlignment="Center" Grid.Column="1" Margin="5,0"/>

            <ProgressBar x:Name="pbHealth" Height="30" Margin="10,0" Grid.Column="2" VerticalAlignment="Center"/>
            <TextBlock x:Name="tbHP" Text="100/100" FontSize="20" Foreground="White" VerticalAlignment="Center" Grid.Column="3" Margin="10,0"/>

            <StackPanel Orientation="Horizontal" Grid.Column="4" Margin="20,0,0,0">
                <TextBlock Text="Оружие:" FontSize="18" Foreground="White" VerticalAlignment="Center"/>
                <TextBlock x:Name="tbWeapon" FontSize="18" Foreground="Yellow" Margin="8,0,0,0" VerticalAlignment="Center"/>
            </StackPanel>

            <StackPanel Orientation="Horizontal" Grid.Column="5" Margin="30,0,0,0">
                <TextBlock Text="Броня:" FontSize="18" Foreground="White" VerticalAlignment="Center"/>
                <TextBlock x:Name="tbArmor" FontSize="18" Foreground="Cyan" Margin="8,0,0,0" VerticalAlignment="Center"/>
            </StackPanel>
        </Grid>

        <!-- === КАРТИНКА КОМНАТЫ === -->
        <Border Grid.Row="1" BorderBrush="White" BorderThickness="3" Margin="20">
            <Image x:Name="imgRoom" Stretch="Uniform"/>
        </Border>

        <!-- === КНОПКИ АТАКОВАТЬ / ЗАЩИЩАТЬСЯ (теперь правее и ниже) === -->
        <StackPanel x:Name="spActions" Orientation="Horizontal"
                    HorizontalAlignment="Center" VerticalAlignment="Center"
                    Grid.Row="2" Margin="0,10,0,10"/>

        <!-- === НИЗ (Инвентарь + Лог) === -->
        <Grid Grid.Row="3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="350"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <GroupBox Header="Инвентарь" Margin="10,0,10,10">
                <StackPanel x:Name="spInventory" Margin="10"/>
            </GroupBox>

            <GroupBox Header="События" Grid.Column="1" Margin="10,0,10,10">
                <ListBox x:Name="lbLog" Background="#FF111111" Foreground="LightGray" 
                         ScrollViewer.VerticalScrollBarVisibility="Auto"/>
            </GroupBox>
        </Grid>
    </Grid>
</Page>